# @formspree/react still declares a peer range of react@^16.8 || ^17 || ^18,
# while this project is on React 19. The library works fine in practice, but
# npm refuses to resolve the tree without this. Remove once Formspree ships a
# React 19 peer range (or once the newsletter form stops using the library).
legacy-peer-deps=true
